// Unfinished
#include <bits/stdc++.h>
using namespace std;
string msgYes = "%d,%d are closest, %d,%d are most distant.";
string msgNo = "There are no adjacent primes.";

int main()
{
    int kase;
    for (scanf("%d", &kase); kase > 0; kase--)
    {
        long long l, r;
        scanf("%lld%lld", &l, &r);
    }
    return 0;
}
