//UNFINISHED
#include<bits/stdc++.h>
using namespace std;

struct Fraction
{
	Fraction(int u=1,int d=1)
	{
		up=u;
		down=d;
	}
	int up,down;
};

Fraction a,b;
int c;

bool isOK(Fraction f)
{
	/*
	 * if((f+a).down==c || (f+b).down==c || (f*a).down==c || (f*b).down==c)
	 * {
	 * 		return true;
	 * }
	 * 
	 * else
	 * {
	 * 		return isOK(?);
	 * }
	 * */
}

int main()
{
	
}
